class PhoneContact:
    def __init__(self, name, contactNumber, note):
        self.name = name
        self.contactNumber = contactNumber
        self.note = note

        def display(self):
            print(self.name, self.contactNumber, self.note)


class PhoneBook:
    def __init__(self, contactNumber):
        self.contactNumber = contactNumber
        self.contactNumber = []

    def getContactByName(self):
        self.name = print(self.contactNumber)
    def newContact(self):
        input(self.name)
        input(self.contactNumber)
